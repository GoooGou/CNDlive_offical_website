---
import {
  Mail,
  Linkedin,
  Twitter,
  Facebook,
  Youtube,
  Github,
} from 'lucide-react';

interface Props {
  title?: string; // 提示语，默认 "Connect with us"
  contactHref?: string; // 联系页面的路径，默认 "/contact"
  contactText?: string; // 按钮文字，默认 "Contact Us"
}

const {
  title = 'Connect with CNDLive',
  contactHref = '/contact',
  contactText = 'Contact Our Team',
} = Astro.props;

// 🛠️ 配置你的社交媒体链接
const socials = [
  {
    icon: Linkedin,
    href: 'https://linkedin.com',
    label: 'LinkedIn',
    color: 'hover:text-[#0077b5]',
  },
  {
    icon: Twitter,
    href: 'https://twitter.com',
    label: 'Twitter',
    color: 'hover:text-[#1DA1F2]',
  },
  {
    icon: Youtube,
    href: 'https://youtube.com',
    label: 'YouTube',
    color: 'hover:text-[#FF0000]',
  },
  {
    icon: Github,
    href: 'https://github.com',
    label: 'GitHub',
    color: 'hover:text-white',
  },
  // { icon: Facebook, href: "https://facebook.com", label: "Facebook", color: "hover:text-[#1877F2]" },
];
---

{/* not-prose 非常重要：防止 Tailwind Typography 干扰我们的布局样式 */}
<div class="not-prose bg-surface-muted my-12 rounded-2xl p-1">
  <div
    class="flex flex-col items-center justify-between gap-6 rounded-[14px] bg-zinc-950 p-6 md:flex-row md:p-8"
  >
    {/* 左侧：标题 + 社交图标 */}
    <div
      class="flex flex-col items-center gap-4 text-center md:items-start md:text-left"
    >
      <h4 class="m-0 text-lg font-bold text-white">{title}</h4>

      <div class="flex gap-3">
        {
          socials.map((item) => (
            <a
              href={item.href}
              target="_blank"
              rel="noopener noreferrer"
              class={`rounded-full border border-zinc-800 bg-zinc-900 p-2.5 text-zinc-400 transition-all duration-300 hover:scale-110 hover:border-zinc-700 hover:bg-zinc-800 ${item.color}`}
              aria-label={item.label}
            >
              <item.icon className="w-5 h-5" />
            </a>
          ))
        }
      </div>
    </div>

    {/* 右侧：联系按钮 (CTA) */}
    <a
      href={contactHref}
      class="group relative inline-flex items-center gap-2 rounded-full bg-white px-8 py-3 font-bold text-zinc-900 shadow-lg shadow-blue-900/20 transition-all hover:-translate-y-0.5 hover:shadow-blue-500/40"
    >
      <Mail className="h-4 w-4" />
      <span>{contactText}</span>

      {/* 按钮光效 */}
      <div
        class="pointer-events-none absolute inset-0 rounded-full ring-1 ring-white/20 ring-inset"
      >
      </div>
    </a>
  </div>
</div>
