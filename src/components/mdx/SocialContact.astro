---
import {
  Mail,
  Linkedin,
  Twitter,
  Facebook,
  Youtube,
  Github,
  Instagram,
} from 'lucide-react';

interface Props {
  title?: string; // 提示语，默认 "Connect with us"
  contactHref?: string; // 联系页面的路径，默认 "/contact"
  contactText?: string; // 按钮文字，默认 "Contact Us"
}

const {
  title = 'Connect with CNDLive',
  contactHref = '/contact',
  contactText = 'Contact Our Team',
} = Astro.props;

// 🛠️ 配置你的社交媒体链接
const socials = [
  {
    icon: Linkedin,
    href: 'https://linkedin.com/company/cndlive-avoip/',
    label: 'LinkedIn',
    color: 'hover:text-[#0077b5]',
  },
  {
    icon: Facebook,
    href: 'https://www.facebook.com/cndliveAVoIP',
    label: 'Facebook',
    color: 'hover:text-[#1877F2]',
  },
  {
    icon: Youtube,
    href: ' https://www.youtube.com/@CNDLive',
    label: 'YouTube',
    color: 'hover:text-[#FF0000]',
  },
  {
    icon: Instagram,
    href: 'https://www.instagram.com/cndlivee/',
    label: 'Instagram',
    color: 'hover:text-white',
  },
  // { icon: Facebook, href: "https://facebook.com", label: "Facebook", color: "hover:text-[#1877F2]" },
];
---

{/* not-prose 非常重要：防止 Tailwind Typography 干扰我们的布局样式 */}
<div class="not-prose bg-surface-muted my-12 rounded-2xl p-1">
  <div
    class="bg-muted flex flex-col items-center justify-between gap-6 rounded-[14px] p-6 md:flex-row md:p-8"
  >
    {/* 左侧：标题 + 社交图标 */}
    <div
      class="flex flex-col items-center gap-4 text-center md:items-start md:text-left"
    >
      <h4 class="text-text-main m-0 text-lg font-bold">{title}</h4>

      <div class="flex gap-3">
        {
          socials.map((item) => (
            <a
              href={item.href}
              target="_blank"
              rel="noopener noreferrer"
              class={`border-text-muted bg-surface-muted text-text-muted hover:border-text-main hover:bg-text-main rounded-full border p-2.5 transition-all duration-300 hover:scale-110 ${item.color}`}
              aria-label={item.label}
            >
              <item.icon className="w-5 h-5" />
            </a>
          ))
        }
      </div>
    </div>

    {/* 右侧：联系按钮 (CTA) */}
    <a
      href={contactHref}
      class="group bg-surface-muted text-text-main shadow-text-main/20 hover:shadow-text-main/40 relative inline-flex items-center gap-2 rounded-full px-8 py-3 font-bold shadow-lg transition-all hover:-translate-y-0.5"
    >
      <Mail className="h-4 w-4" />
      <span>{contactText}</span>

      {/* 按钮光效 */}
      <div
        class="pointer-events-none absolute inset-0 rounded-full ring-1 ring-white/20 ring-inset"
      >
      </div>
    </a>
  </div>
</div>
