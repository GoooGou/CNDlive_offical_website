// ... (HTML <body> 保持不变)

<style is:global>
  /* 1. 强制设定 HTML/BODY 的基础背景色和文字颜色 */
  html {
    /* 在亮色模式下，确保背景是白色，文字是深色 (Tailwind Gray-800) */
    background-color: white;
    color: #1f2937;
  }

  /* 2. MDX 内容区域的基础样式 */
  /* 如果你的文章正文是普通的 <p>, <h2> 等，请在这里确保它们使用强对比颜色 */
  body {
    /* 继承 HTML 的颜色，并确保字体清晰 */
    font-size: 1rem;
    line-height: 1.75;
  }

  /* 3. 暗色模式覆盖（这是你要求的 CSS 切换逻辑） */
  .dark {
    /* 暗色模式下，背景深灰，文字浅白 */
    background-color: #111827; /* Tailwind Gray-900 */
    color: #e5e7eb; /* Tailwind Gray-200 */
  }

  /* 4. 解决 MDX/H2/H3 标题颜色在亮色模式下太浅的问题 */
  /* 确保标题在亮色模式下是强对比黑色 */
  .dark h1,
  .dark h2,
  .dark h3 {
    color: #e5e7eb;
  }
  h1,
  h2,
  h3 {
    /* 默认模式 (浅色) 下，标题使用几乎纯黑色 */
    color: #1f2937;
  }
</style>
