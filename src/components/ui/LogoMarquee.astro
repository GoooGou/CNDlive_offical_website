---
// src/components/ui/LogoMarquee.astro

const logos = [
  { name: 'Nintendo', icon: '🎮' },
  { name: 'PlayStation', icon: '🕹️' },
  { name: 'Xbox', icon: '❎' },
  { name: 'OBS', icon: '🎥' },
  { name: 'YouTube', icon: '🔴' },
  { name: 'Twitch', icon: '💜' },
  { name: 'Discord', icon: '💬' },
];
---

<div
  class="border-surface-border bg-surface-base w-full overflow-hidden border-y py-10 transition-colors duration-300"
>
  <div class="mb-8 text-center">
    <span class="text-text-muted text-sm font-medium tracking-widest uppercase">
      Trusted by Streamers
    </span>
  </div>

  <div
    class="relative flex overflow-x-hidden mask-[linear-gradient(to_right,transparent,black_10%,black_90%,transparent)]"
  >
    <div class="group flex gap-16 py-2 whitespace-nowrap">
      {/* --- 第一组 Loop --- */}
      <div
        class="animate-infinite-scroll flex gap-16 group-hover:[animation-play-state:paused]"
      >
        {
          logos.map((logo) => (
            <div class="/* [关键修改]: 默认使用主文字色，悬停使用品牌色 */ text-text-main hover:text-primary flex cursor-default items-center gap-3 text-2xl font-bold transition-colors">
              <span class="text-3xl">{logo.icon}</span>
              <span>{logo.name}</span>
            </div>
          ))
        }
      </div>

      {/* --- 第二组 Loop (必须完全一致) --- */}
      <div
        class="animate-infinite-scroll flex gap-16 group-hover:[animation-play-state:paused]"
        aria-hidden="true"
      >
        {
          logos.map((logo) => (
            <div class="/* [关键修改]: 保持一致 */ text-text-main hover:text-primary flex cursor-default items-center gap-3 text-2xl font-bold transition-colors">
              <span class="text-3xl">{logo.icon}</span>
              <span>{logo.name}</span>
            </div>
          ))
        }
      </div>
    </div>
  </div>
</div>
