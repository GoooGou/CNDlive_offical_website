---
import { getCollection } from 'astro:content';
import PostGrid from '../common/PostGrid.astro';
import UniversalTitle from '../common/UniversalTitle.astro';

const posts = await getCollection('solutions');
const sortedPosts = posts.sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
const latestCases = sortedPosts.slice(0, 3);
---

<section class="container mx-auto max-w-7xl px-4 py-24">
  <UniversalTitle title="Solutions" href="/solutions" />

  {/* 🔥 首页直接调用 Grid，并且开启 featured 模式 */}
  {/* 这里没有面包屑，也没有返回按钮，完全独立 */}
  <PostGrid posts={latestCases} basePath="/solutions" layout="grid" />
</section>
