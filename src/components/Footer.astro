---
// src/components/Footer.astro

// 1. 引入 React 交互组件
// ⚠️ 注意：NewsletterForm 包含 useState 逻辑，必须保留为 React

// 2. 引入数据
import { FOOTER_LINKS } from '@/data/footerData';
import  NewsletterForm  from '@/components/Newsletter.astro';

const currentYear = new Date().getFullYear();

// 3. 定义一个简单的 Astro 内部组件来渲染列 (替代原来的 FooterColumn)
// 这样做的好处是：链接部分是纯 HTML，不需要浏览器下载 React 代码来渲染它
const { company, products, solutions, support } = FOOTER_LINKS;
---

<footer class="bg-surface-muted text-white pt-20 pb-8 relative overflow-hidden">
  
  {/* --- 背景装饰 (红色弧线) --- */}
  {/* 左上角弧线 */}
  <div class="absolute top-0 -left-20 w-64 h-64 border border-primary/20 rounded-full opacity-50 pointer-events-none"></div>
  {/* 右侧弧线 */}
  <div class="absolute top-40 -right-20 w-96 h-96 border-t border-primary/30 rounded-full opacity-50 pointer-events-none rotate-45"></div>

  <div class="max-w-7xl mx-auto px-6 lg:px-8 relative z-10">
    
    {/* 🔥 关键点：订阅区域 
        使用 client:visible 或 client:load 让 React 组件在浏览器端“活”过来 
    */}
      <NewsletterForm client:visible />
    {/* 2. 链接网格区域 */}
    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-x-8 gap-y-12 border-t border-surface-border pt-16 my-12">
      
      {/* 使用 Astro 原生语法渲染列 (比 React 更快) */}
      
      {/* Column 1: Company */}
      <div class="flex flex-col">
        <h4 class="text-white font-bold mb-4 text-base tracking-wide">{company.title}</h4>
        <ul class="space-y-1">
          {company.items.map((item) => (
            <li>
              <a href={item.href} class="text-sm text-text-muted hover:text-white transition-colors duration-200 block py-1">
                {item.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      {/* Column 2: Products */}
      <div class="flex flex-col">
        <h4 class="text-white font-bold mb-4 text-base tracking-wide">{products.title}</h4>
        <ul class="space-y-1">
          {products.items.map((item) => (
            <li>
              <a href={item.href} class="text-sm text-text-muted hover:text-white transition-colors duration-200 block py-1">
                {item.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      {/* Column 3: Solutions */}
      <div class="flex flex-col">
        <h4 class="text-white font-bold mb-4 text-base tracking-wide">{solutions.title}</h4>
        <ul class="space-y-1">
          {solutions.items.map((item) => (
            <li>
              <a href={item.href} class="text-sm text-text-muted hover:text-white transition-colors duration-200 block py-1">
                {item.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      {/* Column 4: Support */}
      <div class="flex flex-col">
        <h4 class="text-white font-bold mb-4 text-base tracking-wide">{support.title}</h4>
        <ul class="space-y-1">
          {support.items.map((item) => (
            <li>
              <a href={item.href} class="text-sm text-text-muted hover:text-white transition-colors duration-200 block py-1">
                {item.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      {/* Column 5: 关于 Email List 的文字说明 + 社交图标 */}
      <div class="col-span-2 md:col-span-1 lg:col-span-1">
        <h4 class="text-white font-bold mb-4 text-base tracking-wide">Stay Connected</h4>
        <p class="text-sm text-text-muted leading-relaxed mb-4">
          Want to learn more from CNDLive about new product announcements and updates?
          <br />
          Visit our <a href="/contact" class="text-primary hover:underline">Contact page</a> for more info.
        </p>
        
        {/* 这里复用 React 组件来显示图标 (因为 Lucide 图标在 React 里很好用) */}
        {/* 不需要 client:load，因为它是纯展示的 */}
      </div>

    </div>

    {/* 3. 底部版权 */}
    <div class="border-t border-surface-border pt-8 flex flex-col md:flex-row justify-center items-center gap-4 text-xs text-text-muted">
      <p>Copyright © {currentYear} CNDLive All Rights Reserved.</p>
      <a href="/privacy" class="hover:text-white transition underline underline-offset-2">Privacy Policy</a>
    </div>

  </div>
  
  {/* Chat Widget (纯 HTML/CSS 实现，不需要 React) */}
  <div class="fixed bottom-6 right-6 z-50 group">
    <div class="bg-surface-card text-text-main px-4 py-3 rounded-lg shadow-lg mb-3 text-sm font-medium animate-fade-in-up hidden group-hover:block transition-all">
        👋 Hi! How can I help you?
    </div>
    <a href="/contact" class="float-right w-12 h-12 bg-primary rounded-full flex items-center justify-center shadow-lg hover:scale-110 transition-transform relative cursor-pointer">
      {/* 红点状态 */}
      <div class="w-3 h-3 bg-green-500 rounded-full absolute top-0 right-0 border-2 border-primary"></div>
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M20 2H4C2.9 2 2 2.9 2 4V22L6 18H20C21.1 18 22 17.1 22 16V4C22 2.9 21.1 2 20 2Z" fill="white"/>
      </svg>
    </a>
  </div>

</footer>