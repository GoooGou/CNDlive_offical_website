---
import Hero from '@/components/Hero.astro';
import Layout from '@/layouts/Layout.astro';
import { HeroCarousel } from '@/components/hero/HeroCarousel.tsx';
// 1. 引入类型
import type { HeroData } from '@/components/hero/themePresets.ts';
import { slidesData } from '@/components/hero/slidesData'; // 👈 引入刚才抽离的数据
import Main from '@/components/Main.astro';
import Features from '@/components/features/Features.astro';
import LogoMarquee from '@/components/ui/LogoMarquee.astro';
import Intro from '@/components/sections/Intro.astro';
import { Reveal } from '@/components/ui/Reveal';
import NewsSection from '@/components/sections/NewSection.astro';
import CaseSection from '@/components/sections/CaseSection.astro';
import SolutionSection from '@/components/sections/SolutionSection.astro';
---

<Layout title="CNDLive home - Streaming the Moment ">
  <Main>
    {/* 1. Hero 轮播图 - 首屏关键内容，保持 client:load */}
    <Reveal client:load layout="full">
      <HeroCarousel client:load slides={slidesData} />
    </Reveal>

    {/* 2. Logo 跑马灯 - 改为 client:idle 延迟加载 */}
    <Reveal client:idle layout="full">
      <LogoMarquee />
    </Reveal>

    {/* 3. 介绍文字 - 改为 client:idle */}
    <Reveal client:idle layout="full">
      <Intro />
    </Reveal>

    {/* 4. 新闻列表 - 改为 client:idle */}
    <Reveal client:idle layout="narrow">
      <NewsSection />
    </Reveal>
    {/* 6. 解决方案列表 - 改为 client:idle */}
    <Reveal client:idle layout="narrow">
      <SolutionSection />
    </Reveal>
    {/* 7. 案例列表 - 改为 client:idle */}
    <Reveal client:idle layout="narrow">
      <CaseSection />
    </Reveal>
    {/* 5. 特性 BentoGrid - 改为 client:idle */}
    <Reveal client:idle layout="full">
      <Features />
    </Reveal>
  </Main>
</Layout>
