---
// src/pages/404.astro
import ContentLayout from '@/layouts/ContentLayout.astro';
import { Home, Mail, BookOpen } from 'lucide-react';
import Lottie from '@/components/ui/Lottie.astro';
import SocialMediaButton from '@/components/ui/SocialMediaButton.astro';

const title = "404 - Signal Lost";
---

<ContentLayout title={title}>
  <div class="min-h-[80vh] flex flex-col items-center justify-center text-center px-4 py-16">
    
    {/* 1. 插画区域 + 舞台灯光特效 */}
    <div class="mb-8 relative group flex items-center justify-center">
      
      {/* 🔴 舞台红色光晕 (Spotlight) 
          - absolute: 绝对定位，叠在动画下面
          - -z-10: 层级设为负数，确保在动画背后
          - opacity-0 dark:opacity-100: 浅色模式隐藏，深色模式显示
          - bg-red-600/40: 红色的光，带透明度
          - blur-[100px]: 极高的模糊度，模拟散开的光线
      */}
      <div 
        class="
          absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2
          w-[120%] h-[120%] rounded-full
          bg-red-600/0 dark:bg-red-600/40
          blur-[60px] md:blur-[100px]
          -z-10
          transition-opacity duration-700
          pointer-events-none
        "
      ></div>

      {/* 🐶 Lottie 动画
          - dark:invert: 关键！深色模式下把黑色小狗变成白色。
          - mix-blend-overlay: 可选，如果你想让小狗稍微透出一点红色背景的质感，可以加这个，或者不加保持纯白。
      */}
      <Lottie 
        src="/animations/404-2.json" 
        class="w-72 h-72 md:w-80 md:h-80 drop-shadow-xl  dark:drop-shadow-[0_0_30px_rgba(255,0,0,1)]" 
      />
    </div>

    {/* 2. 文字提示 */}
    <div class="space-y-4 max-w-lg mx-auto mb-12 relative z-10">
      <h1 class="text-4xl md:text-5xl font-bold text-text-main tracking-tight">
        信号丢失
      </h1>
      <p class="text-xl text-text-muted">
        抱歉，您访问的频道（页面）不存在或已被移除。<br/>
        请检查链接是否正确，或尝试重新连接。
      </p>
    </div>

    {/* 3. 三个操作按钮 */}
    <div class="flex flex-col sm:flex-row gap-4 w-full sm:w-auto relative z-10">
      
      {/* 按钮 A */}
      <a 
        href="/" 
        class="inline-flex items-center justify-center gap-2 px-8 py-3 rounded-full bg-primary text-white font-bold hover:bg-primary-dark transition-transform hover:scale-105 shadow-lg shadow-primary/20"
      >
        <Home className="w-5 h-5" />
        返回主站
      </a>

      {/* 按钮 B */}
      <a 
        href="/support" 
        class="inline-flex items-center justify-center gap-2 px-8 py-3 rounded-full bg-surface-card border border-surface-border text-text-main font-medium hover:border-primary hover:text-primary dark:hover:bg-white/5 transition-colors"
      >
        <BookOpen className="w-5 h-5" />
        文档中心
      </a>

      {/* 按钮 C */}
      <a 
        href="/contact" 
        class="inline-flex items-center justify-center gap-2 px-8 py-3 rounded-full bg-surface-card border border-surface-border text-text-main font-medium hover:border-primary hover:text-primary dark:hover:bg-white/5 transition-colors"
      >
        <Mail className="w-5 h-5" />
        联系支持
      </a>

    </div>

    {/* 底部小字 */}
    <div class="mt-16 text-sm text-text-muted opacity-60">
     <SocialMediaButton
    </div>

  </div>
</ContentLayout>